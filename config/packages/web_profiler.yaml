when@dev:
    web_profiler:
        toolbar: true

    framework:
        profiler: true

when@test:
    framework:
        profiler:
            collect: false
            collect_serializer_data: true
